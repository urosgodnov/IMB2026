# Block 4 — model pull request (fallback)

If the live GitHub step stalls, show this as "what good looks like".

**PR title:** Add monthly sales report for Adriatica orders

**PR description:**
> This pull request adds `sales_report.py`, which reads `data/orders.csv`,
> computes revenue (`quantity * unit_price`) per month, and prints a monthly
> sales summary. Verified against the known anchor: the best month is
> December 2024 (holiday peak). A short `README.md` documents how to run it.
>
> Generated with Claude Code; reviewed and verified by <your name>.
